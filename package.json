{
  "name": "upstream-typescript-bun",
  "version": "0.0.5",
  "private": true,
  "packageManager": "bun@1.2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "run:deps": "bun run build && node dist/main.js"
  },
  "dependencies": {
    "zod": "^3.24.1",
    "typescript": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
